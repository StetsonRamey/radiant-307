---js
{
  layout: "base.webc",
  pagination: {
    data: "services",
    size: 1,
    alias: "service"
  },
  permalink: function(data) {
    const currentService = data.service;
    return `/services/${this.slugify(currentService.title)}/index.html`;
  },
  eleventyComputed: {
     title: data => `${data.service.title} | Radiant 307 Plumbing`
  }
};

---

<h1 @text="service.title">Default</h1>
<p @text="service.description">Default</p>
